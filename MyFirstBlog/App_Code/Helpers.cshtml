@helper Tags(System.Web.Mvc.HtmlHelper htmlHelper,
    IList<MyFirstBlog.Core.Objects.Tag> tags)
    {
foreach (var tag in tags)
{
        <div class="tags-div">
            @MyFirstBlog.ActionLinkExtensions.TagLink(htmlHelper, tag)
        </div>
}
    }